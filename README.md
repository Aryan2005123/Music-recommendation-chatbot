# 🎵 AI-Based Music Recommendation Chatbot

An interactive **Music Recommendation Chatbot** built using **Python (Tkinter)** and **Natural Language Processing (NLP)**.  
It suggests songs based on the user's mood — like *Happy*, *Sad*, *Energetic*, *Relax*, *Romantic*, *Party*, or *Inspirational*.

---

## 🧠 Features
- GUI built with **Tkinter**
- Detects mood from user input using **NLTK + WordNet**
- One-click **mood buttons** for quick song suggestions
- **Clickable YouTube links** for instant music access
- Highlights the mood inside chat interface
- Supports **chat history** storage using JSON
- Fullscreen mode with **Escape key** to exit

---

## 🛠️ Technologies Used
- **Python 3**
- **Tkinter** (GUI)
- **NLTK (WordNet)** – Natural Language Processing
- **Regex (re)** – for detecting URLs
- **JSON & OS** – for history and file handling
- **Webbrowser** – for opening YouTube links

---

## ⚙️ Installation
1. Clone the repository:
   ```bash
   git clone https://github.com/<your-username>/Music-Recommendation-Chatbot.git
